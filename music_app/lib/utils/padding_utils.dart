import 'package:flutter/material.dart';

class PaddingUtils {
  //BROWSE PAGE
  static const browseTextFieldPadding = EdgeInsets.symmetric(horizontal: 16);
  static const browseBarPadding =
      EdgeInsets.only(left: 16, bottom: 16, right: 16);
  static const allPageSongMainPadding =
      EdgeInsets.only(bottom: 16, left: 16, right: 16);
  static const topBarPadding = EdgeInsets.only(
    top: 12,
    left: 16,
    right: 16,
  );
}
